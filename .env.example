# =============================================================================
# Corp DB Manager 환경 변수 설정
# Version: v1.3.1 (2026-02-08)
# =============================================================================
# 사용법: 이 파일을 .env로 복사 후 값 수정
#   cp .env.example .env
# =============================================================================

# ============================================================
# Server
# ============================================================
HOST=0.0.0.0
PORT=8000
DEBUG=true

# ============================================================
# Database (메타 정보 저장용 SQLite)
# ============================================================
META_DATABASE_URL=sqlite:///./data/meta.db

# ============================================================
# Database (사용자 정보 - PostgreSQL)
# ============================================================
# Docker 환경에서는 코드가 자동으로 host.docker.internal로 전환
DB_HOST=localhost
DB_PORT=5432
DB_NAME=handsdb
DB_USER=postgres
DB_PASSWORD=your-postgres-password

# ============================================================
# Security
# ============================================================
# 반드시 변경! 생성: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=480

# ============================================================
# Default Admin (초기 관리자 계정)
# ============================================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@1234

# ============================================================
# 이메일 설정 (Gmail SMTP)
# ============================================================
# Gmail 앱 비밀번호 발급: Google 계정 → 보안 → 2단계 인증 → 앱 비밀번호
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_NAME=DB 관리 시스템

# ============================================================
# 애플리케이션 URL (이메일 인증 링크용)
# ============================================================
# 프로덕션에서는 실제 도메인으로 변경
APP_URL=http://localhost:8000
